server:
  port: 8095
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
zuul:
  ignored-services: "*"
  routes:
    fares:
      path: /fare-service/**
      url: http://fare-service
management:
  endpoints:
    web:
      exposure:
        include: "*" 
